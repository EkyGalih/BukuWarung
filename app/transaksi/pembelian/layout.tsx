export const metadata = {
    title: "Transaksi | Pembelian"
}

const LayoutPembelian = ({children}: {children: React.ReactNode} ) => {
  return (
    <div className="px-10 py-10">{children}</div>
  )
}

export default LayoutPembelian