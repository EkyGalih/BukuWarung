export const metadata = {
    title: "Transaksi | Penjualan",
};

const PenjualanList = ({ children }: { children: React.ReactNode }) => {
    return (
        <div className="py-10 px-10">{children}</div>
    )
}

export default PenjualanList