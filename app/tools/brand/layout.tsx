export const metadata = {
    title: "Tools | Brand"
}

const LayoutBrand = ({ children }: { children: React.ReactNode }) => {
    return (
        <div className="px-2 py-2">{children}</div>
    )
}

export default LayoutBrand